specVersion: 0.0.2
description: DAOCreator
repository: https://github.com/graphprotocol/example-subgraph
schema:
  file: schema.graphql
dataSources:
  - kind: ethereum/contract
    name: MolochSummoner
    network: ropsten
    source:
      address: "0xc99181d13EAC0CFd7AA45E87aa6DccB610E14650"
      abi: MolochSummoner
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.5
      language: wasm/assemblyscript
      entities:
        - Dao
      abis:
        - name: MolochSummoner
          file: MolochSummoner\abis\MolochSummoner.json
      eventHandlers:
        - event: SummonComplete(indexed
            address,address[],address[],uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256[])
          handler: handleNewDAO
      file: MolochSummoner\MolochSummoner.wasm
